# module01-spring-test ?? ?????? ??
#
# ? ??? ?? ? ?? ???? ???? ?? ??? ????.
# ??? ????? src/test/resources/application.yaml? ?? ????.

# ?? ??
server:
  port: 8080

# Spring ?? ??
spring:
  # ?????? ??
  application:
    name: module01-spring-test

  # ?????? ?? (Section 03 JPA ????)
  datasource:
    url: jdbc:h2:mem:calculatordb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  # H2 ?? ?? (???)
  h2:
    console:
      enabled: true
      path: /h2-console

  # JPA ?? (Section 03??? ??)
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  # SQL ??? ??
  sql:
    init:
      mode: never

# ?? ??
logging:
  level:
    com.wanted.springtest: INFO
    org.springframework: WARN
    org.hibernate: WARN